export const itemCommentsConfig = {
    title: 'item:titles.comments',
};

export const commentCardLabelsConfig = {
    user: 'item:comments.labels.user',
    created: 'item:comments.labels.created',
};

export const formButtonsConfig = {
    newComment: 'item:comments.form.buttons.send',
};

export const tooltipsConfig = {
    delete: 'item:comments.tooltips.delete',
};
