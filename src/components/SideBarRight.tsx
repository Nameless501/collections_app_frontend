import { FC } from 'react';
import SideBarWrapper from './SideBarWrapper';
import { Box } from '@mui/material';

const SideBarRight: FC = () => {
    return (
        <SideBarWrapper anchor="right">
            <Box>
                
            </Box>
        </SideBarWrapper>
    );
};

export default SideBarRight;
