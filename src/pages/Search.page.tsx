import { FC } from 'react';
import FlexCenterWrapper from '../components/FlexCenterWrapper';

const SearchPage: FC = () => {
    return <FlexCenterWrapper align="flex-start"></FlexCenterWrapper>;
};

export default SearchPage;
